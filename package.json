{
	"name": "joinotify-otp-login",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"build:app": "npm run build --prefix app",
		"build:zip": "powershell -ExecutionPolicy Bypass -File scripts/build-plugin-zip.ps1",
		"release": "npm run build:app && npm run build:zip"
	}
}